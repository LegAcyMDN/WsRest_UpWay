@model IEnumerable<WsRest_UpWay.Models.EntityFramework.Assurance>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TitreAssurance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionAssurance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrixAssurance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TitreAssurance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionAssurance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrixAssurance)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AssuranceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AssuranceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AssuranceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
